// "Verlo en tu habitación": AR Quick Look on iOS (spec 9.5). Filled in by M5; hidden where unsupported.
import type { SpeciesId } from '../../engine/species';

export function QuickLookButton(_props: { species: SpeciesId; label: string }) {
  return null;
}
